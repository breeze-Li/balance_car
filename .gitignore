# ================================================
# Keil MDK 工程专用忽略规则
# ================================================

# --- 1. 用户专属配置（极易冲突，必须忽略）---
*.uvoptx          # 用户工程选项（调试器配置、断点、观察点等）
*.uvguix.*        # 用户界面布局文件（窗口位置、大小等）
*.uvopt           # Keil uVision4 及更早版本的用户选项文件
*.uvgui.*         # Keil uVision4 及更早版本的界面布局文件
*.ini             # 调试脚本/初始化文件（可能包含本地硬件路径）
*.jlink           # J-Link 调试配置文件
*.scvd            # Keil 5.25+ 版本的环境配置文件

# --- 2. 编译输出文件（可从源码重新生成）---
*.o               # 目标文件
*.obj             # 目标文件（Windows风格）
*.axf             # ELF格式可执行文件（含调试信息）
*.hex             # HEX烧录文件
*.bin             # 二进制镜像文件
*.map             # 内存映射文件
*.lst             # 汇编列表文件
*.crf             # 交叉引用文件
*.d               # 编译依赖文件
*.lnp             # 链接器路径文件
*.dep             # 依赖关系文件
*.plg             # 编译日志文件
*.htm             # 编译输出报告（如函数调用关系）
*.build_log.htm   # 构建日志

# --- 3. Keil 默认输出目录---
**/Objects/       # 编译中间文件目录（任意深度匹配）
**/Listings/      # 列表文件目录（任意深度匹配）
MDK-ARM/          # Keil 默认构建输出目录

# --- 4. 备份与临时文件---
*.bak             # 备份文件
*.log             # 日志文件
*.tmp             # 临时文件
*.~*              # 临时备份文件
JLinkLog.txt      # J-Link 调试日志

# --- 5. 多项目工作区文件（视情况保留）---
*.uvmpw           # 多项目工作区文件（多人协作时建议忽略）